# Antenna Project

## Predicting with the surrogate model

With the data in the 'data' folder, train the model once by executing the 'train.py' script, load the trained model, and then use the 'predict()' function in 'surrogate' to predict. 